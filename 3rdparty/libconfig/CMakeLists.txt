PROJECT(libconfig)

CHECK_INCLUDE_FILE(xlocale.h HAVE_XLOCALE_H)

IF (HAVE_XLOCALE_H)
	ADD_DEFINITIONS(-DHAVE_XLOCALE_H=1)
ENDIF (HAVE_XLOCALE_H)

FILE(GLOB HEADER_FILES *.h)
FILE(GLOB SOURCE_FILES *.c)

ADD_LIBRARY(config OBJECT ${SOURCE_FILES})
